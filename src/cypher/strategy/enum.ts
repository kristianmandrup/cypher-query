export enum ClauseType {
  unknown = "unknown",
  match = "match",
  where = "where",
  return = "return",
}

export enum WhereFilterType {
  none = "none",
  optional = "optional",
  must = "must",
}
